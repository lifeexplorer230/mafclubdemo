# Правила работы Claude Code для проекта MafClubScore

## 🔢 АВТОМАТИЧЕСКОЕ ВЕРСИОНИРОВАНИЕ

**ВАЖНО**: При каждом коммите в этом проекте ОБЯЗАТЕЛЬНО выполнять автоматическое обновление версии!

### Правило версионирования

**ПОСЛЕ создания коммита**, но **ДО push**, всегда выполнять:

```bash
node scripts/bump-version.js
```

Скрипт автоматически:
1. Определит тип изменения по сообщению коммита
2. Обновит версию в `package.json`
3. Обновит fallback версию в `api/version.js`
4. Добавит запись в `ROADMAP.md` (секция CHANGELOG)
5. Предложит добавить изменения через `git commit --amend`

### Типы версионирования (SemVer)

Скрипт автоматически определяет тип по коммиту:

- **MAJOR** (1.0.0 → 2.0.0) - Breaking changes
  - Коммиты содержащие: `BREAKING`, `major:`
  - Пример: `"BREAKING: Remove old API endpoints"`

- **MINOR** (1.0.0 → 1.1.0) - Новые фичи
  - Коммиты начинающиеся с: `feat:`, `feature:`, `minor:`
  - Пример: `"feat: Add JWT authentication"`

- **PATCH** (1.0.0 → 1.0.1) - Багфиксы и мелкие изменения
  - Все остальные коммиты: `fix:`, `chore:`, `docs:`, `style:`
  - Пример: `"fix: Correct version link to login.html"`

### Стандартный workflow для коммита

```bash
# 1. Обычный коммит изменений
git add -A
git commit -m "feat: Add new feature XYZ"

# 2. ОБЯЗАТЕЛЬНО: Запустить версионирование
node scripts/bump-version.js

# 3. Добавить изменения версии в коммит
git add -A
git commit --amend --no-edit

# 4. Push (опционально с тегом)
git push
git push --tags  # Если хотите создать release
```

### Ручное управление версией

Можно явно указать тип:

```bash
node scripts/bump-version.js patch   # 1.0.0 → 1.0.1
node scripts/bump-version.js minor   # 1.0.0 → 1.1.0
node scripts/bump-version.js major   # 1.0.0 → 2.0.0
```

### Changelog в ROADMAP.md

Каждая версия автоматически записывается в `ROADMAP.md`:

```markdown
## CHANGELOG

### v1.0.8 (2025-01-14)
**Тип**: Patch
**Изменения**: fix: Correct version link to login.html
```

---

## 📋 ПРАВИЛА КОММИТОВ

Используйте conventional commits для автоматического версионирования:

- `feat:` - новая функциональность (minor bump)
- `fix:` - исправление бага (patch bump)
- `docs:` - изменения в документации (patch bump)
- `style:` - форматирование кода (patch bump)
- `refactor:` - рефакторинг без изменения функционала (patch bump)
- `test:` - добавление/исправление тестов (patch bump)
- `chore:` - обновление зависимостей, конфигов (patch bump)
- `BREAKING:` или `major:` - breaking changes (major bump)

---

## 🔗 ССЫЛКА НА ВЕРСИЮ

Кликабельная версия внизу всех страниц (через `shared/version-loader.js`) ведёт на **login.html**.

Это сделано специально для быстрого доступа к авторизации.

---

## 🚀 DEPLOYMENT

При деплое на Vercel обязательно установить environment variable:

```
APP_VERSION=v1.0.7
```

Версия будет автоматически отображаться на всех страницах через API `/api/version`.

---

## ✅ CHECKLIST ПЕРЕД КАЖДЫМ PUSH

- [ ] Код протестирован локально (`npm test`)
- [ ] Коммит создан с правильным префиксом (feat:/fix:/etc.)
- [ ] **Запущен `node scripts/bump-version.js`**
- [ ] Изменения версии добавлены в коммит (`git commit --amend`)
- [ ] ROADMAP.md обновлён с changelog
- [ ] Push выполнен

---

**Помни**: Версионирование - это не опция, а обязательная часть workflow!
